module.exports = {
example: `

para usar $setTimeout:

$setTimeout[
Seu incrivel codigo aqui
;tempo aqui (se for em ms coloque o numero inteiro);identificador do timeout]

Para parar o timeout:
$clearTimeout[identificador do timeout]

`
};
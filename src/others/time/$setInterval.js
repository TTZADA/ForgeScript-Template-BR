module.exports = {
example: `

para usar $setInterval:

$setInterval[
Seu incrivel codigo aqui
;tempo aqui (se for em ms coloque o numero inteiro);identificador do intervalo]

Para parar o intervalo:
$clearInterval[identificador do intervalo]

`
};